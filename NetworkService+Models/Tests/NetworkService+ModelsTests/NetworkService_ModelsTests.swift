import XCTest
@testable import NetworkService_Models

final class NetworkService_ModelsTests: XCTestCase {
    func testExample() {
        // This is an example of a functional test case.
        // Use XCTAssert and related functions to verify your tests produce the correct
        // results.
        XCTAssertEqual(NetworkService_Models().text, "Hello, World!")
    }

    static var allTests = [
        ("testExample", testExample),
    ]
}
